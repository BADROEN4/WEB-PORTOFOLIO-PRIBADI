{
    "version": 2,
    "framework": "laravel",
    "functions": {
        "api/index.php": {
            "runtime": "vercel-php@0.6.0"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index.php"
        }
    ],
    "installCommand": "npm install && npm run build && composer install --no-dev",
    "buildCommand": "php artisan cache:clear && php artisan config:cache && php artisan route:cache && php artisan view:cache"
}
